export interface IMaxMinDTO {
  max_confirm: number;
  max_death: number;
  max_incidence: number | null;
  max_isolation: number;
  max_lethality: number | null;
  max_mortality: number | null;
  med_confirm: number;
  med_death: number;
  med_incidence: number | null;
  med_isolation: number;
  med_lethality: number | null;
  med_mortality: number | null;
  min_confirm: number;
  min_death: number;
  min_incidence: number | null;
  min_isolation: number;
  min_lethality: number | null;
  min_mortality: number | null;
}
